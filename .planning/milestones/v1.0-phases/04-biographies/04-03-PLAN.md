---
phase: 04-biographies
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - BIO-01
  - BIO-02
  - BIO-03
  - BIO-04
must_haves:
  truths:
    - "PM confirms the full browse-to-bio user flow works on desktop"
    - "PM confirms the full browse-to-bio user flow works on a real iOS device"
    - "PM confirms bio page content is accurate and links reach the correct Church pages"
    - "GitHub Pages deployment succeeds — all 15 bio pages accessible at production URL"
  artifacts:
    - path: "build/bios/[slug]/index.html"
      provides: "Pre-rendered HTML for all 15 bio pages"
      contains: "15 subdirectories in build/bios/"
  key_links:
    - from: "GitHub Pages"
      to: "build/bios/"
      via: "GitHub Actions deploy workflow"
      pattern: "gh-pages branch"
---

<objective>
Deploy the biography feature to GitHub Pages and obtain PM verification on desktop and real iOS device.

Purpose: Confirms the static prerendering, base-path prefixing, and outbound link behaviour all work correctly in the production environment — not just dev server.
Output: Live verified biography section at https://[user].github.io/prophets/bios
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-biographies/04-01-SUMMARY.md
@.planning/phases/04-biographies/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify static output</name>
  <files></files>
  <action>
    Run the production build and verify all 15 bio pages were emitted correctly.

    1. Run: `cd /home/void/projects/prophets && npm run build`
    2. Verify 15 bio subdirectories exist: `ls /home/void/projects/prophets/build/bios/`
    3. Spot-check that a bio page contains expected content (name, sourceUrl): `grep -i "churchofjesuschrist" /home/void/projects/prophets/build/bios/dallin-h-oaks/index.html | head -3`
    4. Verify the /bios directory page was built: `ls /home/void/projects/prophets/build/bios/index.html`

    If build fails: check for missing `entries()` in +page.js (most likely cause — see RESEARCH.md Pitfall 1). If any bio page subdirectory is missing, the entries() function is not returning that slug.
  </action>
  <verify>
    <automated>cd /home/void/projects/prophets && npm run build && ls build/bios/ | wc -l</automated>
  </verify>
  <done>
    - npm run build exits with code 0
    - build/bios/ contains 16 entries (1 index.html + 15 slug subdirectories)
    - Each slug subdirectory contains index.html
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PM verification — desktop and iOS</name>
  <what-built>
    Complete biography section:
    - /bios directory page: all 15 leaders grouped by quorum, each card linking to /bios/[slug]
    - /bios/[slug] bio pages: breadcrumb, photo, quick-facts panel, Official Biography link, Conference Talks link
    - Nav: Biographies link is now active (was greyed out)
    - All deployed to GitHub Pages (production)
  </what-built>
  <how-to-verify>
    Deploy first (push to main — GitHub Actions will deploy automatically).

    **Desktop verification (production URL https://[your-username].github.io/prophets/bios):**
    1. Visit /prophets/bios — confirm all 15 leaders appear in two groups (First Presidency: 3, Quorum of Twelve: 12)
    2. Click any leader card — confirm it navigates to /prophets/bios/[slug]
    3. On the bio page, confirm: breadcrumb shows "Biographies › [Name]", photo shows, Quick Facts panel shows born/age/called/spouse/children/career
    4. Click "Official Biography" — confirm it opens the correct Church page in a new tab
    5. Click "Conference Talks" — confirm it opens the Conference talks index in a new tab (or gracefully shows the Church's page if no talks exist yet for a new leader)
    6. Click "Biographies" in the breadcrumb — confirm it navigates back to /prophets/bios
    7. Verify the Biographies link in the nav is blue/active (not grey)

    **iOS verification (real device — not Chrome DevTools):**
    8. Open https://[your-username].github.io/prophets/bios on a real iOS device
    9. Confirm leader cards are tappable and navigate to the bio page
    10. Confirm outbound links open in a new tab (not replacing current page)
    11. Confirm breadcrumb "Biographies" link taps back correctly

    **Content spot-check:**
    12. Verify 2-3 bio pages for factual accuracy — name, title, birth date, calling date should match churchofjesuschrist.org
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
- All 15 bio pages accessible at production GitHub Pages URL
- PM has verified desktop and real iOS user flow
- All outbound links open in new tab
- No broken images or 404 links (within our control)
</verification>

<success_criteria>
- npm run build produces 15 bio page HTML files
- PM approves the desktop and iOS user flow at the production URL
- The biography section is live and browsable at https://[username].github.io/prophets/bios
</success_criteria>

<output>
After completion, create `/home/void/projects/prophets/.planning/phases/04-biographies/04-03-SUMMARY.md`
</output>
