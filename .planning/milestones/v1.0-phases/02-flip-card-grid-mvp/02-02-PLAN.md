---
phase: 02-flip-card-grid-mvp
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [GRID-01, GRID-02, GRID-03]

must_haves:
  truths:
    - "All 15 leader photos are visible in the grid with no broken images"
    - "Hovering any card on desktop triggers a smooth 3D flip revealing name and title"
    - "Tapping a card on a touch device flips it; tapping again unflips it"
    - "Flipping one card does not affect any other card"
    - "No layout overflow or horizontal scroll on any viewport width"
  artifacts:
    - path: "src/lib/components/LeaderCard.svelte"
      provides: "Verified flip card component — both faces render correctly"
    - path: "src/routes/+page.svelte"
      provides: "Verified two-section grid — all 15 cards visible and interactive"
  key_links:
    - from: "browser (desktop hover)"
      to: "@media (hover: hover) CSS rule"
      via: "CSS hover state on .scene"
      pattern: "hover: hover"
    - from: "browser (touch tap)"
      to: "Svelte $state flipped toggle"
      via: "onclick handler on .scene"
      pattern: "toggleFlip"
---

<objective>
Visually verify the flip card grid works correctly on both desktop (hover) and touch (tap) before Phase 2 is marked complete.

Purpose: Catch iOS Safari quirks, broken images, layout issues, and animation glitches that automated build checks cannot detect.
Output: PM approval (or issue list for fix iteration).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-flip-card-grid-mvp/02-CONTEXT.md
@.planning/phases/02-flip-card-grid-mvp/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dev server and confirm build is clean</name>
  <files></files>
  <action>
Run the development server so the PM can preview the flip card grid locally before any device testing.

```bash
cd /home/void/projects/prophets && npm run dev
```

The dev server starts at http://localhost:5173 (or the next available port — check terminal output).

Also confirm the production build is clean:
```bash
cd /home/void/projects/prophets && npm run build 2>&1 | grep -E "error|warn|✓|built" | head -20
```

If the build has errors, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /home/void/projects/prophets && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Build exits 0 with no errors. Dev server URL reported in terminal output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Responsive flip card grid replacing the placeholder leader list:
    - Two labeled sections: "First Presidency" (3 cards) and "Quorum of the Twelve Apostles" (12 cards)
    - Cards sorted by seniority within each section
    - Portrait photo on the front face (full-bleed, no text overlay)
    - Thumbnail + name + title on the back face
    - Desktop: hover flips, unhover returns
    - Mobile/touch: tap flips, tap again unflips
    - Grid: 2 cols mobile / 3 cols tablet / 4 cols desktop
  </what-built>
  <how-to-verify>
    Open http://localhost:5173 (check terminal for exact port if different).

    **Desktop checks (in browser with mouse):**
    1. Confirm all 15 leader photos are visible — no broken image icons
    2. Confirm two section headings: "First Presidency" and "Quorum of the Twelve Apostles"
    3. Hover over any card — confirm it flips smoothly and shows name + title on back
    4. Move mouse away — confirm card returns to photo face
    5. Verify hover on one card does not affect any other card
    6. Resize browser to ~500px wide — confirm 2-column layout with no horizontal overflow

    **Touch device checks (real iOS or Android device — not DevTools emulation):**
    7. Open http://[your-local-IP]:5173 on the device (replace with your machine's local IP)
    8. Tap any card — confirm it flips to show name + title
    9. Tap the same card again — confirm it unflips back to photo
    10. Tap multiple different cards — confirm each flips independently

    **Visual quality (Claude's discretion items — flag if obviously wrong):**
    - Card proportions look portrait-oriented (taller than wide)
    - Back face text is readable (not too small, not mirrored/backwards)
    - Animation is smooth (not jerky or flickering)
    - Cards use reasonable spacing — not cramped, not excessively gapped
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g., "back face text is mirrored on iPhone", "3 cards show broken images")</resume-signal>
</task>

</tasks>

<verification>
PM approval confirms:
1. All 15 leader portraits visible (GRID-01)
2. Desktop hover flip works smoothly (GRID-02)
3. Mobile tap flip and unflip works on a real device (GRID-03)
</verification>

<success_criteria>
PM types "approved" after verifying all 15 leaders are visible, hover flip works on desktop, and tap flip works on a real touch device. Phase 2 is complete when this checkpoint passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-flip-card-grid-mvp/02-02-SUMMARY.md`
</output>
